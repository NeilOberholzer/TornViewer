import { RouterModule } from '@angular/router';
import { TornUserComponent } from './torn/torn-user.component';

export const routing = RouterModule.forRoot([
    {path: '', component: TornUserComponent}
]);
